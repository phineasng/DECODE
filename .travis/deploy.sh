#!/usr/bin/env bash

# some steps to (build and) upload your built artifacts from travis.
# this might include steps to
# - install cli tools to interact with services (e.g. ibmcloud, openshift, ...)
# - log in to the registy
# - tag image/images accordingly for pushing them
# - actually pushing/uploading the images
# - trigger other events now that these images are available